Set these environment variables for deployment to azure app service

AZURE_STORAGE_CONNECTION_STRING = [actual connection string]
AZURE_STORAGE_CONTAINER = [actual container name]
STORAGE_TYPE = azure
